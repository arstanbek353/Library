import spinner from './spinner.gif'

const Spinner = () => (<div className="Spinner" style={{ display: 'flex', alignItems: 'center', justifyContent: 'center' }}><img src={spinner} alt="" /></div>)

export default Spinner